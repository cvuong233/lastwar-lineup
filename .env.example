# R4 login credentials (checked server-side now, not just a UI gate)
EDITOR_USERNAME=tusengland
EDITOR_PASSWORD=tus1234

# Random long string used to sign session tokens. Generate with:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=change-me-to-a-long-random-string
# Optional: session lifetime in seconds (default 30 days)
JWT_TTL_SECONDS=2592000

# GitHub - fine-grained PAT scoped to ONLY this repo, Contents: Read and write
GITHUB_TOKEN=github_pat_xxx
GITHUB_OWNER=cvuong233
GITHUB_REPO=agent-presentation
GITHUB_PATH=lastwar/players.json
GITHUB_BRANCH=master
